Reserved for low-level UI primitives (e.g. a future shadcn/ui or Radix integration)
if the project adopts one. Empty in the scanned structure — left empty here rather
than guessing its intended contents. The concrete components (Button, Input, etc.)
live as siblings of this folder, not inside it.
